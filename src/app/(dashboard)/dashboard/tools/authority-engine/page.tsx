import { AuthorityEngineContent } from "@/components/AuthorityEngineContent";

export const dynamic = "force-dynamic";

export const metadata = {
    title: "Authority Engine | AI for Boomers",
    description: "Generate professional LinkedIn posts, newsletters, and cold emails from your stories.",
};

export default function AuthorityEnginePage() {
    return <AuthorityEngineContent />;
}
